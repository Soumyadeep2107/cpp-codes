#include<bits/stdc++.h>
using namespace std;
int main()
{
    unordered_set<int>st;
    //lower_bound and upper_bound doesn't work here 
    //rest all the functions are same.
    //as the name suggests it doesn't stores element in any partiular order 
    // only thing is the elements should be unique
    // it has better time comlexity , like in most of cases it has ,O(1) complexity tham set.
    // except some collision happens and in worst case it can give time complexity of O(n).

}