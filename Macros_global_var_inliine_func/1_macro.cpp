#include<bits/stdc++.h>
using namespace std;
#define PI 3.14 // no semi-colon required at the end
int main()
{
    // a piece of code which is replaced by the macro(if defined), before the runtime of the program.And this macro does'nt take any type of space in memory and also immutable in he whole program.
    int rad= 5;
    cout<<double(PI*pow(rad,2));
}