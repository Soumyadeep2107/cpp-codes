#include<bits/stdc++.h>
using namespace std;
int main()
{
    /*int num=5;
    cout<<num<<endl;
    cout<<&num<<endl; //&- is the address of operator,which tells the address of a variable.
    int *ptr=&num;    // pointer is a variable which stores the address of the variabe of some data  time. int *ptr can be read as a pointer to the int data type.
    // * is a deferencig operator , i.e when *ptr is printed it prints the value at the ptr or address .... and ptr have some address of variable which have the value.
    cout<<ptr<<endl;
    cout<<*ptr<<endl;    // prints the value 5.
    cout<<sizeof(ptr)<<endl;
    cout<<sizeof(num)<<endl;
    int *m=0;
    cout<<sizeof(m);*/
    
    int arr[]={23,45,67,102};
    cout<<arr[3]<<endl;
    int i=3;
    cout<<i[arr]<<endl;
} 
